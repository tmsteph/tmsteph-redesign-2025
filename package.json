{
  "name": "tmsteph-redesign-2025",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "python3 -m http.server 8000",
    "test": "vitest run",
    "test:e2e": "NODE_OPTIONS='--require ./scripts/playwright-termux-platform.cjs' playwright test",
    "test:e2e:headed": "NODE_OPTIONS='--require ./scripts/playwright-termux-platform.cjs' playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "jsdom": "^24.0.0",
    "typescript": "^5.5.4",
    "vitest": "^1.6.0"
  }
}
